
  # ASSET TRACKER

  This is a code bundle for ASSET TRACKER. The original project is available at https://www.figma.com/design/bGwFQw1S3DCPm7EHAJmN62/ASSET-TRACKER.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  